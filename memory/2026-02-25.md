# 2026-02-25

## TODO Tonight
- ซื้อ Mac mini M4 วันนี้
- คืนนี้: ย้าย OpenClaw ทั้งหมดไป Mac mini ใหม่ รัน 24/7
- ขั้นตอน: install Node → install openclaw → copy workspace + config → ตั้ง launchd → ทดสอบ Telegram

## Context Imported from Zzz Workspace (/Users/sanwithz/Documents/Zzz)

### User Identity
- Name: Kru Bank, Sittichart
- Telegram: 6796212791
- Timezone: Asia/Bangkok
- Dedicated Google account: `opensanwithz@gmail.com`

### Financial Safety Rule (HIGHEST PRIORITY)
- Never initiate, confirm, or enable any payment/purchase/subscription/auto-renew/trial-to-paid/wallet/card action without explicit written user approval first.
- Always provide transparent step logs for anything that could spend or bind.

### Key Workflows & Triggers

**Trading:**
- Trigger: "Trade หาค่า API" or "Trade เพื่อหาค่า API"
  1. Open TradingView chart link from user
  2. Capture chart screenshot (indicators + status panel)
  3. Analyze trend bias, momentum, indicator context
  4. Return 30m trade plan with: Plan A (trend-follow) + Plan B (reclaim/reversal)
  5. Include exact levels: Entry zone, SL, TP1-TP3, risk guidance
  6. Append/update Trading Logger in the same run
- Logged first win: BTCUSDT SHORT 25x, Entry 67,653.29, Exit 67,061.4, PnL: +15.38 USDT (2026-02-23 07:49:51)

**Gold Price:**
- When user asks about gold price ("อยากรู้ราคาทองคำ"):
  1. Capture header crop: `h3.h-h3` title from goldtraders.or.th
  2. Capture table crop: price table block
  3. Merge header+table vertically via ffmpeg (`vstack`)
  4. Send ONLY the final merged image to Telegram
  - Do NOT send full-page screenshots, redesigns, or re-rendered versions

**Flowchart:**
- Always use the manual fallback method:
  1. Generate local HTML/CSS flowchart file
  2. Open in browser
  3. Capture screenshot
  4. Send image to Telegram

**Boris-style Dev Workflow (non-trivial tasks):**
  1. Deep research → `research.md`
  2. Detailed planning → `plan.md`
  3. Annotation cycle ("don't implement yet")
  4. Granular `todo.md`
  5. Implement ONLY after explicit plan approval

### UI Style Preferences (Apps Script / Frontend)
- Layout: `max-w-5xl` container, 3-column grid, primary form card (`md:col-span-2`, `rounded-2xl`, `shadow-soft`, `border-slate-100`)
- Inputs: mono-style gray fields (`pl-10 pr-4 py-2.5 bg-gray-50 border-gray-200 rounded-lg text-sm focus:ring-2 focus:ring-gray-900 font-mono`)
- Submit: brand blue rounded button with icon (`bg-brand-500`, `hover:bg-brand-700`)
- Sidebar: `bg-white rounded-2xl border border-slate-100 p-5 md:p-6`
- ApexCharts legend CSS: `.apexcharts-legend-series.apexcharts-no-click { cursor: auto; }` + hide zero/null series

### Projects in Zzz Workspace
- `kanban-board/index.html` — daily summary dashboard
- `kanban-board/trading-logger.html` — yearly dot visualization + KPI
- `Trading Simulator/index.html` — OHLC chart with tabs + local indicator engine
- `scripts/sim-alert-bridge.mjs` — Telegram alert forwarding service
- `outputs/` — trade plans, gold price images
- `memory/trading-stats.csv` — trade history ledger

### Tools Installed (from Zzz)
- `summarize` CLI — brew install steipete/tap/summarize
- `html-to-markdown` CLI — $HOME/go/bin/html2markdown
- Scripts: `scripts/url2md.sh`, `scripts/url2obsidian.sh`
- Nano Banana Pro (Gemini image gen) — uses `inlineData` (camelCase!)

### Social Channels (Reference)
- TikTok: https://www.tiktok.com/@opensanz
- YouTube: https://www.youtube.com/@opensanwithz

### Trading Config
- Capital context: $115 balance, 25x Coin-M leverage (simulation)
- Indicator stack: Master Candle breakout + EMA 12/26/200
- Heikin Ashi 30m candles (converted from OHLC before indicator calc)
- RR ratio: 1:2.5
