# 2026-02-22

- User preference locked: For future flowchart requests, use the manual fallback method that works reliably:
  1) Generate a local HTML/CSS flowchart layout file
  2) Open it in browser
  3) Capture screenshot
  4) Send image back to Telegram
- Trigger phrase context: "ถ้าให้ทำ flowchart อีกในอนาคตให้ใช้วิธีการนี้"
- Workflow standard approved by user:
  - Use Boris-style coding workflow for non-trivial dev tasks:
    1) Deep research -> `research.md`
    2) Detailed planning -> `plan.md`
    3) Annotation cycle with user notes, explicit "don't implement yet"
    4) Granular `todo.md`
    5) Implement only after explicit plan approval
- Skill base updated from user-provided React+Gemini frontend instruction doc:
  - Added `docs/REACT_GEMINI_SKILL_BASE.md` operational rules
  - Added `templates/react_gemini_app_checklist.md` pre-delivery QA checklist
  - Enforces correct `@google/genai` usage + Tailwind-only + React 18 TS structure
- Skill base updated from Antigravity-style agentic instruction doc:
  - Added `docs/AGENTIC_SKILL_BASE_UPDATE.md`
  - Adapted identity to Orange 🍊 and OpenClaw runtime
  - Adopted planning/execution/verification artifact discipline
  - Excluded incompatible environment-specific constraints (Windows/.gemini/task_boundary tools)
- User granted a dedicated Google account for assistant operations: `opensanwithz@gmail.com`.
  - Use this account for Gmail communication and Google services/App Script tasks per user instruction.
  - Keep credentials/private access confidential.
- User designated social channels for assistant learning/reference:
  - TikTok profile: `https://www.tiktok.com/@opensanz`
  - YouTube channel: `https://www.youtube.com/@opensanwithz`
  - Instruction: open and remember these channels as primary sources to learn from their video content.
- Trading workflow trigger saved:
  - Trigger phrase: **"Trade หาค่า API"**
  - Required flow each time:
    1) Open TradingView chart link provided by user
    2) Capture chart screenshot (chart region with indicators/status panel)
    3) Analyze current state (trend bias, momentum/indicator context)
    4) Return actionable 30m trade plan with exact levels:
       - Plan A (trend-follow)
       - Plan B (reclaim/reversal)
       - Entry zone, SL, TP1-TP3, risk guidance
    5) Keep response concise and execution-focused
- Gold price capture preference locked:
  - When user asks about gold price ("อยากรู้ราคาทองคำ"), send only one final image containing:
    1) Header crop: `h3.h-h3` title area (ref often e16)
    2) Table crop: price table block (ref often e17)
    3) Merge header+table vertically into one image via ffmpeg (`vstack`)
  - Send only the final merged image to Telegram (no intermediate images).
  - Do NOT send full-page website screenshots.
  - Do NOT redesign/re-render/reformat the gold board. Use the exact site visual (crop + merge only).
