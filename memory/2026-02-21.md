# 2026-02-21

- First direct Telegram interaction with user 6796212791.
- User introduced themselves as "Kru Bank, Sittichart".
- User asked for ongoing help in these areas:
  - Creating files (Word, PDF, Excel, PowerPoint)
  - Writing/editing (reports, emails, scripts, code)
  - Research (web search + summaries)
  - Building apps (Google Apps Script web apps, automation)
  - Organizing/analyzing data, files, and tasks
- UI style preference saved for future Apps Script forms:
  - Layout: `max-w-5xl` container with 3-column grid and primary form card (`md:col-span-2`, `rounded-2xl`, `shadow-soft`, `border-slate-100`)
  - Inputs/Textarea: mono-style gray field classes (`pl-10 pr-4 py-2.5 bg-gray-50 border-gray-200 rounded-lg text-sm focus:ring-2 focus:ring-gray-900 ... font-mono`)
  - Select: matching gray class style with `pl-4 pr-10`
  - Submit button: brand blue rounded button with icon (`bg-brand-500`, `hover:bg-brand-700`)
  - Sidebar/aside style: white card (`bg-white rounded-2xl border border-slate-100 p-5 md:p-6`) with heading `font-semibold mb-3 flex items-center gap-2`, Lucide chart icon (`bar-chart-3`, `w-4 h-4`), and chart container `#chart` with `h-64` (min-height ~260px)
  - Note: prefer clean source markup for aside (no pasted runtime-generated Apex SVG/canvas DOM blobs)
  - ApexCharts legend CSS preferences:
    - `.apexcharts-legend-series.apexcharts-no-click { cursor: auto; }`
    - Hide zero/null legend series with `display: none !important`
    - `.apexcharts-inactive-legend { opacity: 0.45; }`
  - Chart/aside structure preference confirmed: keep a clean source `<aside>` + `#chart` container + helper text; avoid storing runtime-generated ApexCharts DOM/SVG payloads from browser inspection (foreignObject/paths/tooltips markup)
- User established a strict "Locked Purse" policy as top priority:
  - No payments/spending/subscriptions/auto-renew/trial-to-paid actions without explicit written consent in advance.
  - No silent charges or hidden purchase flows.
  - Assistant may prepare and advise, but must pause before any money movement until explicit approval is given.
  - Transparency and auditable steps are mandatory for spend/binding actions.
