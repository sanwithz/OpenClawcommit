#!/usr/bin/env bash
set -euo pipefail
VENV_BIN="/Users/harvey/.openclaw/workspace/.venvs/notebooklm-py/bin/notebooklm"
if [[ ! -x "$VENV_BIN" ]]; then
  echo "NotebookLM CLI not found at: $VENV_BIN" >&2
  exit 1
fi
exec "$VENV_BIN" "$@"
